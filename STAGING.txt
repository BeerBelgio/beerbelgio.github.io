V0.55.1.7 STAGING
Desktop resize root-cause fix: the canonical 760px shell now has an explicit minmax(0,1fr) grid track so intrinsic child widths cannot expand the cards beyond the shell. Desktop scale is continuous at 45% of the current layout viewport, with proportional symmetric gutters at every width. Mobile, FULL LAVA copy, sonoDGTL and frozen V0.54.4 lava physics are unchanged.
